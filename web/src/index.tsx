import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

/* componentes em react são funções que retornam um conteúdo HTML */
/* componentes sempre devem conter a primeira letra maiúscula */
/* sempre que passar um atributo para um componente, podemos chamar de propriedade */
/* utilizando o react router dom para a criação das rotas de paginação da aplicação */
/* utilizar o leaflet para as rotas caso não queira pagar pelo google maps */
